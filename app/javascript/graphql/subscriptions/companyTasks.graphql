subscription companyTasksSubscription {
  companyTasks {
    hasRespondSchedules {
      nodes {
        ...ScheduleItem
      }
    }
    waitingSchedules {
      nodes {
        ...ScheduleItem
      }
    }
    confirmedScheduleTasks {
      nodes {
        ...ScheduleItem
      }
    }
  }
}
