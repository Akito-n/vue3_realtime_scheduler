subscription individualTasksSubscription {
  individualTasks {
    blankScheduleTasks {
      nodes {
        id
        name
        companyName
      }
    }
    waitingTasks {
      nodes {
        ...ScheduleItem
      }
    }
    respondingTasks {
      nodes {
        ...ScheduleItem
      }
    }
    confirmedScheduleTasks {
      nodes {
        ...ScheduleItem
      }
    }
  }
}
