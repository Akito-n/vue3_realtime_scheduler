subscription schedulesSubscription($occupationIds: [ID!]!) {
  schedules(occupationIds: $occupationIds) {
    schedules {
      nodes {
        id
        startAt
        endAt
        mine
        isRequest
        status
        requester {
          id
          name
          color
          companyName
        }
      }
    }
  }
}
